services:
  anythingllm-sso:
    build: .
    environment:
      - API_KEY=Z7VCSWY-CAR4YC7-J8608ND-1EQ9AP4
      - USER_ID=2
      - PORT=3000
      - WORKSPACE_SLUG=jonathan-pi-assistant
      - CLEANUP_ENABLED=true
      - CLEANUP_CRON=0 0 * * *
    restart: unless-stopped
    labels:
      - traefik.enable=true
      - traefik.http.routers.askredirect.rule=Host(`askredirect.johnnypie.work`)
      - traefik.http.routers.askredirect.entrypoints=web,websecure
      - traefik.http.routers.askredirect.tls=true

networks:
  default:
    name: traefik_default
    external: true